
#include <bits/stdc++.h>
/*
if x values of both key points are same then chose y value
low==high then one elmt in buiding arr then return height which is created by 2 key elmts
o.w recursivly divide the skyline & made the o/p -
*/
using namespace std;
class Solution
{
public:
    vector<vector<int>> getSkyline(vector<vector<int>> &buildings)
    {
        vector<vector<int>> ans;
    }
};
