#include <stdio.h>

int main(void) {
	// your code goes here
	int x,y,a,b,t;
	scanf("%d",&t);
	while(t--)
	{
	    int cnt=0;
	    scanf("%d",&cnt);
        a!=b;
        x!=y;
	scanf("%d%d%d%d",&x,&y,&a,&b);
	if(x==a || x==b)
	printf("%d\n",cnt++);
	
	else
	if(y==a || y==b)
	printf("%d\n",cnt++);
	else
	printf("%d\n",cnt);
	}
	
	return 0;
}
