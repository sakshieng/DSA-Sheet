#include <stdio.h>

int main(void) {
	// your code goes here
	int t;
	scanf("%d",&t);
	while(t--)
	{
	    long long int n , m;
	    int i;
	    char s[n];
	    char s[m];
	    scanf("%lld%lld",&n,&m);
	    for(i=0;i<n;i++)
	    {
	    if(s[n]=='?')
	   {
	       printf("%c\n",s[n]='a' || s[n]='b' || s[n]='c'|| s[n]='d'|| s[n]=='e');
	   }
	   else
	    if(s[m]=='?')
	   {
	       printf("%c\n",s[m]='a' || s[m]='b' || s[m]='c'|| s[m]='d'|| s[m]=='e');
	   }
	   else
	   if(s[n]=='?' && s[m]=='?')
	   printf("")
	    }
	}
	return 0;
}

